default:
    suites:
        integration:
            paths:
                - %paths.base%/features/integration/

            contexts:
                - ApplicationContext
                - GithubContext
                - JiraContext
                - TempoContext

            filters:
              tags: '@cli'
